import 'package:flutter/material.dart';

class HowToArrive extends StatelessWidget {
  const HowToArrive({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('ezay aro7 screen')),
    );
  }
}
