import 'package:flutter/material.dart';

class FreeScreen extends StatelessWidget {
  const FreeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('free screen')),
    );
  }
}
